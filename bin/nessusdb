#!/usr/bin/env ruby
# encoding: utf-8

$LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '/../lib'))

$stdout.sync = true
$stderr.sync = true

require 'rubygems'
require 'nessusdb'

app = NessusDB::CLI::Application.new
app.run
